export const updateBounds = (bounds) => ({
  type: "UPDATE_BOUNDS",
  bounds
});

export const updateTag = (tag) => ({
  type: "UPDATE_TAG"
});

export const removeTags = () => ({
  type: "REMOVE_TAGS"
});
